@model IEnumerable<Movies.Data.Entities.MoviesClass>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MovieName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MovieGenere)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MovieReleaseYear)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MovieCollectionAmount)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MovieName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MovieGenere)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MovieReleaseYear)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MovieCollectionAmount)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                @Html.ActionLink("Details", "Details", new { id=item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Id })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
